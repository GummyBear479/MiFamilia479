# Start Dev Server

1. Check if dependencies are installed (`node_modules` exists), if not run `npm install`.
2. Read package.json to confirm the dev script.
3. Run `npm run dev` (or equivalent).
4. Confirm the server is listening and report the URL.
